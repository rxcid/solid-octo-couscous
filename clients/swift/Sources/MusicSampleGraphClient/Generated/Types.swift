// Generated by swift-openapi-generator, do not modify.
@_spi(Generated) public import OpenAPIRuntime
#if os(Linux)
@preconcurrency public import struct Foundation.URL
@preconcurrency public import struct Foundation.Data
@preconcurrency public import struct Foundation.Date
#else
public import struct Foundation.URL
public import struct Foundation.Data
public import struct Foundation.Date
#endif
/// A type that performs HTTP operations defined by the OpenAPI document.
public protocol APIProtocol: Sendable {
    /// Find the catalog recordings for a recognized song
    ///
    /// Pass a catalog id, ISRC, MusicBrainz recording id, or title and artist. The first stages are Sinc's bundled lookup: canonical id, then the union of ISRC and MusicBrainz matches, then normalized title and artist. Known title and artist aliases are a last exact stage that only the online catalog has.
    ///
    /// - Remark: HTTP `GET /v1/tracks/resolve`.
    /// - Remark: Generated from `#/paths//v1/tracks/resolve/get(resolveTrack)`.
    func resolveTrack(_ input: Operations.ResolveTrack.Input) async throws -> Operations.ResolveTrack.Output
    /// A track, its credits, and its other versions
    ///
    /// - Remark: HTTP `GET /v1/tracks/{id}`.
    /// - Remark: Generated from `#/paths//v1/tracks/{id}/get(getTrack)`.
    func getTrack(_ input: Operations.GetTrack.Input) async throws -> Operations.GetTrack.Output
    /// What the song takes from, and what takes from it
    ///
    /// Covers every version of the song. One relationship per related song, direction, and type, preferring the one that says what was borrowed. Only published relationships.
    ///
    /// - Remark: HTTP `GET /v1/tracks/{id}/relationships`.
    /// - Remark: Generated from `#/paths//v1/tracks/{id}/relationships/get(getRelationships)`.
    func getRelationships(_ input: Operations.GetRelationships.Input) async throws -> Operations.GetRelationships.Output
    /// The song's lineage, generation by generation
    ///
    /// A tree of relationships from the song outward, as Sinc's lineage view builds it: sources by default (what it samples, what those sample, and so on), or derivatives. Nodes come depth-first with parentId set, so a client can rebuild the tree in one pass.
    ///
    /// - Remark: HTTP `GET /v1/tracks/{id}/lineage`.
    /// - Remark: Generated from `#/paths//v1/tracks/{id}/lineage/get(getLineage)`.
    func getLineage(_ input: Operations.GetLineage.Input) async throws -> Operations.GetLineage.Output
    /// The song's sound family, following sampled, interpolated and remixed handoffs
    ///
    /// Matches Sinc's GenerationsBuilder over published catalog edges and version clusters. One handoff is one generation; covers do not count.
    ///
    /// - Remark: HTTP `GET /v1/tracks/{id}/generations`.
    /// - Remark: Generated from `#/paths//v1/tracks/{id}/generations/get(getGenerations)`.
    func getGenerations(_ input: Operations.GetGenerations.Input) async throws -> Operations.GetGenerations.Output
    /// Other songs built from the same sources
    ///
    /// Grouped by shared source, busiest first. `total` counts every song before trimming.
    ///
    /// - Remark: HTTP `GET /v1/tracks/{id}/siblings`.
    /// - Remark: Generated from `#/paths//v1/tracks/{id}/siblings/get(getSiblings)`.
    func getSiblings(_ input: Operations.GetSiblings.Input) async throws -> Operations.GetSiblings.Output
    /// Search songs by title and artist
    ///
    /// Every word must appear in the title or artist, so "juicy notorious" works. One recording per song.
    ///
    /// - Remark: HTTP `GET /v1/search`.
    /// - Remark: Generated from `#/paths//v1/search/get(searchTracks)`.
    func searchTracks(_ input: Operations.SearchTracks.Input) async throws -> Operations.SearchTracks.Output
}

/// Convenience overloads for operation inputs.
extension APIProtocol {
    /// Find the catalog recordings for a recognized song
    ///
    /// Pass a catalog id, ISRC, MusicBrainz recording id, or title and artist. The first stages are Sinc's bundled lookup: canonical id, then the union of ISRC and MusicBrainz matches, then normalized title and artist. Known title and artist aliases are a last exact stage that only the online catalog has.
    ///
    /// - Remark: HTTP `GET /v1/tracks/resolve`.
    /// - Remark: Generated from `#/paths//v1/tracks/resolve/get(resolveTrack)`.
    public func resolveTrack(
        query: Operations.ResolveTrack.Input.Query = .init(),
        headers: Operations.ResolveTrack.Input.Headers = .init()
    ) async throws -> Operations.ResolveTrack.Output {
        try await resolveTrack(Operations.ResolveTrack.Input(
            query: query,
            headers: headers
        ))
    }
    /// A track, its credits, and its other versions
    ///
    /// - Remark: HTTP `GET /v1/tracks/{id}`.
    /// - Remark: Generated from `#/paths//v1/tracks/{id}/get(getTrack)`.
    public func getTrack(
        path: Operations.GetTrack.Input.Path,
        headers: Operations.GetTrack.Input.Headers = .init()
    ) async throws -> Operations.GetTrack.Output {
        try await getTrack(Operations.GetTrack.Input(
            path: path,
            headers: headers
        ))
    }
    /// What the song takes from, and what takes from it
    ///
    /// Covers every version of the song. One relationship per related song, direction, and type, preferring the one that says what was borrowed. Only published relationships.
    ///
    /// - Remark: HTTP `GET /v1/tracks/{id}/relationships`.
    /// - Remark: Generated from `#/paths//v1/tracks/{id}/relationships/get(getRelationships)`.
    public func getRelationships(
        path: Operations.GetRelationships.Input.Path,
        query: Operations.GetRelationships.Input.Query = .init(),
        headers: Operations.GetRelationships.Input.Headers = .init()
    ) async throws -> Operations.GetRelationships.Output {
        try await getRelationships(Operations.GetRelationships.Input(
            path: path,
            query: query,
            headers: headers
        ))
    }
    /// The song's lineage, generation by generation
    ///
    /// A tree of relationships from the song outward, as Sinc's lineage view builds it: sources by default (what it samples, what those sample, and so on), or derivatives. Nodes come depth-first with parentId set, so a client can rebuild the tree in one pass.
    ///
    /// - Remark: HTTP `GET /v1/tracks/{id}/lineage`.
    /// - Remark: Generated from `#/paths//v1/tracks/{id}/lineage/get(getLineage)`.
    public func getLineage(
        path: Operations.GetLineage.Input.Path,
        query: Operations.GetLineage.Input.Query = .init(),
        headers: Operations.GetLineage.Input.Headers = .init()
    ) async throws -> Operations.GetLineage.Output {
        try await getLineage(Operations.GetLineage.Input(
            path: path,
            query: query,
            headers: headers
        ))
    }
    /// The song's sound family, following sampled, interpolated and remixed handoffs
    ///
    /// Matches Sinc's GenerationsBuilder over published catalog edges and version clusters. One handoff is one generation; covers do not count.
    ///
    /// - Remark: HTTP `GET /v1/tracks/{id}/generations`.
    /// - Remark: Generated from `#/paths//v1/tracks/{id}/generations/get(getGenerations)`.
    public func getGenerations(
        path: Operations.GetGenerations.Input.Path,
        headers: Operations.GetGenerations.Input.Headers = .init()
    ) async throws -> Operations.GetGenerations.Output {
        try await getGenerations(Operations.GetGenerations.Input(
            path: path,
            headers: headers
        ))
    }
    /// Other songs built from the same sources
    ///
    /// Grouped by shared source, busiest first. `total` counts every song before trimming.
    ///
    /// - Remark: HTTP `GET /v1/tracks/{id}/siblings`.
    /// - Remark: Generated from `#/paths//v1/tracks/{id}/siblings/get(getSiblings)`.
    public func getSiblings(
        path: Operations.GetSiblings.Input.Path,
        query: Operations.GetSiblings.Input.Query = .init(),
        headers: Operations.GetSiblings.Input.Headers = .init()
    ) async throws -> Operations.GetSiblings.Output {
        try await getSiblings(Operations.GetSiblings.Input(
            path: path,
            query: query,
            headers: headers
        ))
    }
    /// Search songs by title and artist
    ///
    /// Every word must appear in the title or artist, so "juicy notorious" works. One recording per song.
    ///
    /// - Remark: HTTP `GET /v1/search`.
    /// - Remark: Generated from `#/paths//v1/search/get(searchTracks)`.
    public func searchTracks(
        query: Operations.SearchTracks.Input.Query,
        headers: Operations.SearchTracks.Input.Headers = .init()
    ) async throws -> Operations.SearchTracks.Output {
        try await searchTracks(Operations.SearchTracks.Input(
            query: query,
            headers: headers
        ))
    }
}

/// Server URLs defined in the OpenAPI document.
public enum Servers {}
